# Tchess is terminal based chess game

- to start the game run `python3 tchess.py`

- to move a piece pick a position that consist of four characters ex:`a2a3`

- rules are the same as chess rules with **less features** and no win condition for now 
 
- type `restart` to restart the game session and `quit` or `exit` to exit the game 

